# FB_PC_V1.2 实现说明

> 读者：维护 / 二次开发本工程的同学。配套的运行操作见同目录《使用说明.md》。

## 1. 项目定位

在 Windows PC 上用 Chrome 自动批量播放 Facebook 视频页面，同时在**本机（PC）与 IAS 设备**双侧抓包，记录每个视频的实际播放画质、播放时长、起止时间、播放 IP、是否被"封堵"等信息，汇总写入 Excel 结果表，并留存 pcap 与 TLS 密钥文件供后续用 Wireshark 解密分析。

V1.2 相对 V1.1 的新增能力：**WiFi 自动重连 / 强制切换到指定 WiFi**（启动时、获取 IP 时、页面打不开时、播放卡住时四个触发点）。

## 2. 工程结构

| 文件 | 职责 | 对应原单文件 FB_PC_V1.2.py 的部分 |
|---|---|---|
| `config.py` | 配置项：路径、WiFi、设备账号、开关（常量，大写命名） | `__main__` 中的变量 + `WIFI_SSID` + `browser()` 里的 `user_data_dir` |
| `utils.py` | 基础方法：日志重定向、WiFi 管理、进程清理、IP 切换、小工具 | 模块级函数（第 23~241 行） |
| `business.py` | 业务逻辑：`FB` 类整体（抓包、浏览器、播放、监控、汇总） | 第 244~1739 行，逐字节原样迁入 |
| `main.py` | 入口：打印 banner → 清理进程 → 连 WiFi → 组装 FB → 启动 | `if __name__ == '__main__':` 块 |

依赖导入关系：`main.py → business.py → utils.py → config.py`（config 不依赖其余三者，无循环导入）。

第三方依赖：`ping3`、`psutil`、`openpyxl`、`paramiko`、`xlsxwriter`、`selenium`、`undetected-chromedriver`。
（原文件中的 `shutil`、`selenium.webdriver.chrome.service.Service`、`webdriver_manager` 为未使用导入，拆分时已去除。）

## 3. 模块说明

### 3.1 config.py（配置项）

| 常量 | 含义 | 默认值 |
|---|---|---|
| `WIFI_SSID` | 自动连接/强制切换的目标 WiFi | `ASUS_F8_5G` |
| `LOG_DIR` | 日志文件目录 | `E:\PC-Facebook` |
| `USER_DATA_DIR` | 浏览器用户数据目录（原写死在 `browser()` 内，目前仅赋值、未实际启用） | `D:\user\23456789` |
| `ROOT_PATH` | 结果输出目录（按启动日期生成） | `E:\PC-Facebook\fb_result\YYYYMMDD` |
| `PCAP_PATH` | 本机抓包目录（按启动日期生成） | `E:\PC-Facebook\fb_pcap\YYYYMMDD` |
| `EXCEL_NAME` | 结果 Excel 文件名（按启动时刻生成） | `ROOT_PATH\fb播放结果_YYYYMMDD_HHMMSS.xlsx` |
| `CREATE_CMD` | 启动时在 IAS 上创建的日志目录命令 | `mkdir /appslog/fb_pc_YYYYMMDD` |
| `WIRESHARK_PATH` | Wireshark 安装目录（调用其 tshark.exe） | `C:\Program Files\Wireshark` |
| `ETH_NAME` | 本机捕包网卡 GUID（\Device\NPF_{...}） | 内网机网卡 GUID |
| `IP_LIST_PATH` | IP 切换列表 Excel | `E:\PC-Facebook\fb_result\ip_list.xlsx` |
| `INTERFACE` | 上网网卡名称 | `WLAN` |
| `TCPDUMP_PATH` / `IAS_ETH` | IAS 侧抓包程序路径 / 捕包网卡 | `/apps/ddriver/tools/dumpcap` / `0` |
| `HOSTNAME` / `USERNAME` / `PASSWORD` | IAS 的 SSH 地址与 root 账号 | `192.168.80.93` / `root` / `******` |
| `FB_USERNAME` / `FB_PASSWORD` | Facebook 登录账号（供 `login()` 使用，当前入口未调用） | — |
| `VID_FILE` | 视频播放列表 Excel | `E:\PC-Facebook\fb_result\fb视频列表_1.xlsx` |
| `IS_SCREEN` | `True`=无界面(headless)运行 | `False` |
| `IS_IAS_PCAP` | 是否开启 IAS 侧抓包 | `True` |
| `IS_TIME_LIMIT` | IP 复用时是否启用 30 分钟限制 | `False` |
| `IS_IP_SWITCH` | 是否允许自动切换 IP | `True` |

路径类常量在**导入 config 时**按当前时间生成（与原脚本在 `__main__` 开头生成等价）。注释掉的备选值（另一台 Wireshark 路径、外网机网卡、另一套 IAS 地址等）均以注释形式保留在文件中。

### 3.2 utils.py（基础方法）

| 函数 | 作用 | 要点 |
|---|---|---|
| `TeeLogger`（类）+ 模块级安装代码 | 把 `sys.stdout` 同时重定向到控制台与日志文件 | **导入 utils 时立即生效**；每次启动生成 `LOG_DIR\fb_log_YYYYMMDD_HHMMSS.log`，并打印日志路径与版本号两行 |
| `get_wifi_ssid(interface)` | 解析 `netsh wlan show interfaces` 取当前 SSID | 未连网时返回 None；GBK 解码 |
| `wait_for_valid_ip(interface, timeout)` | 轮询网卡 IP，排除 APIPA 地址（169.254.x.x） | 每 5 秒查一次，超时返回 None |
| `ensure_wifi_connected(ssid, interface, wait_seconds)` | 已连目标 WiFi→直接返回；连了别的 WiFi→断开后切换；没连→直接连 | 连接前检查 netsh 中是否保存过该 WiFi 配置文件；连上后再等 DHCP 有效 IP |
| `kill_processes(exe_name_list)` | 按进程名遍历 kill（Windows 自动补 .exe） | 用于清理 chrome / tshark |
| `set_ip_address(ip_list_path, interface, ip_filter, is_time_limit)` | 从 IP 列表挑下一个可用 IP 并 `netsh` 设为静态地址 | 见 4.4 节 |
| `time_to_seconds(time_str)` | `mm:ss` / `hh:mm:ss` → 秒 | |
| `get_browser_version()` | 读注册表取 Chrome 大版本号 | 失败时兜底返回 128（供 undetected_chromedriver 使用） |

### 3.3 business.py（FB 类）

`class FB(multiprocessing.Process)` —— 虽继承进程类，但**从未 `.start()`**，入口直接同步调用其方法，实际全部运行在主线程；真正并发的是内部用 `multiprocessing.dummy.Process`（即线程）启动的两个抓包任务。

主要属性：构造参数原样保存（root_path、pcap_path、excel_name、wireshark_path、eth_name、ip_list_path、interface、tcpdump_path、ias_eth、hostname、username、password、is_screen、is_ias_pcap、vid_file、fb_username、fb_password、is_time_limit、is_ip_switch），运行态属性：`ip`、`driver`、`tcpdump_pid`、`cmd`（IAS 抓包命令）、`uc`（是否用 undetected_chromedriver）、`cookies` 等。

| 方法 | 作用 |
|---|---|
| `browser(sslkey_log_path)` | 组装 Chrome 启动参数（分辨率 1920x1080、忽略证书错误、host-resolver 规则屏蔽 optimizationguide 域名等），按 `is_screen` 决定 headless，按 `self.uc` 决定用 uc.Chrome 还是原生 Chrome；`sslkey_log_path` 非空时加 `--ssl-key-log-file` |
| `get_data_dict()` | 读视频列表（B~E 列），vid 为空跳过、时长为空默认 60 秒，返回 `{行号: [vid, 分辨率, 时长, 状态]}` |
| `get_ip(interface)` | 先做 WiFi 检查（V1.2），再取网卡 IP；IP 无效（空/169.254 开头）且允许切换时调 `set_ip_address` 换 IP，二次校验失败返回 None |
| `get_pcap(pcap_name)` | 线程任务：起 tshark 在指定 NPF 网卡上写 pcap（目录不存在则创建） |
| `ias_opt(cmd)` | 线程任务：SSH 到 IAS 执行命令。详见 4.5 节 |
| `_cleanup_ias_capture()` | SSH 上去 `ps aux | grep dumpcap`，逐个 `kill -15`，并清空 `/appslog/runing_pid` |
| `login()` | Facebook 网页登录并保存 cookies（临时启用 uc；**当前入口未调用**，保留备用） |
| `open_fb(vid, play_time_limit, resolution)` | 单个视频的完整播放周期，见 4.2 节 |
| `one_inster()` | 播放主循环，见 4.1 节 |

### 3.4 main.py（入口）

启动时序：导入 utils（触发日志重定向并打印日志路径、版本号）→ 打印 banner → `kill chrome/tshark` → `ensure_wifi_connected` → 构造 FB（19 个参数全部来自 config）→ `is_ias_pcap` 为真时先 `ias_opt(CREATE_CMD)` 在 IAS 上建当日目录 → `one_inster()`。

## 4. 核心流程

### 4.1 整体时序与播放主循环（one_inster）

1. 确保 `root_path`、`pcap_path` 存在（不存在则创建）；
2. 读视频列表 `get_data_dict()`；创建结果 Excel 并写入 13 列表头；
3. 逐行遍历：状态为"已播放"的跳过 → 调 `open_fb()` → 结果追加到 Excel → **无论成功失败**都在视频列表 E 列标记"已播放"（因此中断后重跑会自动续跑，跳过已尝试的视频）；
4. `open_fb` 抛异常时，把 vid 和异常写入 `fb播放失败URL列表_时间戳.txt`。

### 4.2 open_fb 的十一个阶段

| 阶段 | 内容 |
|---|---|
| 1 初始化 | `get_ip()`（WiFi 检查+合理性+按需切换，失败返回 `IP设置不成功` 结果）；生成本机 pcap / IAS pcap / TLS key 文件名（格式 `YYYYMMDD_win10_chrome_{vid}_{分辨率}_{ip}_pc|ias|key`）；组装 IAS 抓包命令（nohup dumpcap `host {ip}`，PID 写入 `/appslog/runing_pid`）；设置环境变量 `SSLKEYLOGFILE` 并验证目录可写 |
| 2 清理残留 | 先清 IAS 上残留 dumpcap（`_cleanup_ias_capture`，kill -15），再清本机 chrome/tshark |
| 3 启动 IAS 抓包 | 线程执行 `ias_opt(抓包命令)`；主线程轮询 PID 文件（每秒，最多 30 秒）拿到 PID 后再等 3 秒；超时返回 `IAS抓包启动失败` |
| 4 启动 PC 抓包 | 线程执行 `get_pcap`（tshark），等 3 秒；记录 `start_time` |
| 5 打开视频页 | 启动浏览器访问 `https://www.facebook.com/watch?v={vid}`；按标题+正文关键词（"无法访问此网站"、ERR_CONNECTION_* 等）判断页面是否可达：**不可达** → 停 IAS 抓包、清理本地、WiFi 重连检查、按需切 IP、返回 `视频链接打开失败`；`WebDriverException` 分支同理并区分超时提示 |
| 6 关登录弹窗 | 依次尝试多种 aria-label（关闭/Close…），失败再按按钮文本（×/✕/关闭/Close）查找，用 ActionChains 悬停后点击 |
| 7 取视频时长 | 优先 slider 元素 `aria-valuemax/aria-valuenow`；退回旧版 span XPath；都失败用 0:00 |
| 8 取当前画质 | 鼠标移到播放器唤出控制栏，按 class 特征找选中项，识别"自动/auto"或 `数字+p`；默认"自动" |
| 9 切预设画质 | 打开 设置→画质 菜单，遍历选项（忽略大小写及 P 后缀匹配预设分辨率），命中则点其父级 button；不命中则保持当前画质 |
| 10 异常分支 | 若弹窗未关闭成功**或**未取到时长：不做画质操作，原地等待预设时长后关闭，结果按 画质=自动、实际时长=预设时长 返回 |
| 11 正常播放与监控 | JS 检测暂停状态并尝试播放（空格键、k 键、UI 按钮多级兜底）→ 监控循环（每秒）：①进度 60 秒无变化判为"封堵"，做 WiFi 检查、按需切 IP、跳出；②slider→JS→DOM 三级取播放进度；③鼠标移动保持控制栏可见；④当前进度 ≥ 总时长（且总时长>0）即认为播完跳出 → 收尾（关浏览器 → kill tshark → IAS `kill -15` 并最多验证 10 次确认停止 → 清残余 chrome → 按需切 IP）→ 汇总打印 → 返回结果 |

**返回值（10 元组）**：`(实际播放时长, 视频总时长, 实际画质, 是否封堵, itag(恒为'-'), 开始时间, 结束时间, 播放IP, PCAP文件名, IAS_PCAP文件名)`。
实际播放时长取法：封堵时=最后进度；否则 `预设时长 >= 视频总时长` 取总时长（视频播完），否则取预设时长。

### 4.3 WiFi 自动重连（V1.2 新增）

`ensure_wifi_connected` 三种情形：已连目标 WiFi 直接通过；连了其他 WiFi 先 `netsh wlan disconnect` 再连；未连网直接连。连接动作循环尝试直到 `get_wifi_ssid` 确认连上，随后 `wait_for_valid_ip` 等 DHCP 分配到非 169.254 地址。四个触发点：main 启动时、`get_ip` 发现 SSID 不对时、页面打不开时、播放卡住 ≥60 秒时。**前提**：系统里保存过该 WiFi 的配置文件（手动连过一次并勾选自动连接），否则脚本只报错提示、无法自动连。

### 4.4 IP 切换（set_ip_address）

`ip_list.xlsx` 格式：A=IP，B=网关，C=上次使用时间，D=是否已用（True）。逻辑：从第 2 行起找第一个"未使用"的 IP；`is_time_limit` 为真时还要求其上次使用时间距今超过 30 分钟；候选 IP ping 不通（且不是本机当前域名解析 IP）才认为可用，用 `netsh interface ip set address` 设为静态 IP（掩码 255.255.255.0），写回使用时间与标记。当列表即将用完（剩余 ≤2 行）或遇到异常短 IP 时自动整列重置 D 列，实现循环复用。

### 4.5 IAS 抓包交互（ias_opt）

`is_ias_pcap` 为假直接返回。通过 paramiko SSH 连接 IAS（**注意：socket 源地址写死绑定 `10.10.163.86`**，见第 7 节），执行流程：确保 `/appslog/fb_pc_YYYYMMDD` 目录存在 → 若是抓包命令，先清理残留 dumpcap 进程（kill -9）→ 执行命令；抓包命令启动后读 `/appslog/runing_pid` 里的 PID 存到 `self.tcpdump_pid`；kill 命令则清空该 PID。停止抓包即对其发 `kill -15`，并在 `open_fb` 收尾时用 `ps -p {pid}` 反复验证进程确已消失。

### 4.6 TLS 解密支持

浏览器启动前设置 `SSLKEYLOGFILE` 环境变量并加 `--ssl-key-log-file` 参数，每个视频生成同名 `.key` 文件；Wireshark 中配置该 key 文件即可解密对应 pcap 的 HTTPS 流量。

## 5. 数据文件契约

| 文件 | 格式 |
|---|---|
| `fb视频列表_1.xlsx` | 第 1 行表头；B=视频 vid，C=画质（如 720P/1080P/自动，内部统一去 P 转大写），D=预设播放时长（秒，空则 60），E=状态（"已播放"跳过；播完自动写入） |
| `ip_list.xlsx` | A=IP，B=网关，C=上次使用时间，D=是否已用 |
| 结果 Excel | 13 列：vid、画质、实际播放画质、视频总时长、itag、是否封堵成功、预设时长、实际时长、开始时间、停止时间、播放IP、PCAP文件名、IAS_PCAP文件名 |
| 失败列表 txt | 每次运行一个 `fb播放失败URL列表_时间戳.txt`，追加"vid+异常信息" |
| 日志 | `E:\PC-Facebook\fb_log_时间戳.log`，控制台内容全量同步 |

## 6. 并发模型

- 主流程同步执行；IAS 抓包（`ias_opt`）与本机抓包（`get_pcap`）各由一个**线程**执行（`multiprocessing.dummy.Process`，Windows 下无 spawn 重导入问题）；
- `FB` 继承 `multiprocessing.Process` 仅是历史写法，未调用 `.start()`，不产生子进程；
- 线程与主线程的同步点：`tcpdump_pid` 轮询（阶段 3）、`time.sleep` 固定等待。

## 7. 已知写死点 / 冗余点（迁入时刻意保留，未改行为）

1. SSH socket 源地址绑定写死 `10.10.163.86`（`ias_opt`、`_cleanup_ias_capture`、PID 轮询、停止验证共 4 处）——调试遗留；若本机无此地址，IAS 相关操作会失败。如需修复，可改为绑定 `self.ip`（原代码中留有注释掉的写法）；
2. `open_fb` 中的 `resolution_dict`（分辨率→档位映射）定义后未使用；
3. 结果中 itag 列恒为 `-`；
4. `browser()` 的 `user_data_dir` 仅赋值未使用（现为 `config.USER_DATA_DIR`）；
5. `login()` 未在入口调用（入口有注释 `# fb_obj.login()`），账号密码来自 config；
6. 大量 `print('123')` 类调试输出与 emoji 日志，均为原脚本行为，原样保留。

## 8. 与原单文件版本的关系

本次拆分为**纯结构重构**：business.py 类体、utils.py 函数体与原文件逐字节一致（脚本切片生成并断言）；main.py 逻辑与原 `__main__` 等价；仅 3 处名义变化——`user_data_dir` 移入配置、删除 3 个未使用 import、常量改为大写命名。原 `FB_PC_V1.2.py` 保留在同目录作为对照与回退。
