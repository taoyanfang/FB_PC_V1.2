# FB_PC_V1.2 使用说明

> 读者：运行本脚本的测试 / 操作人员。代码结构细节见同目录《实现说明.md》。

## 1. 功能简介

本工程自动批量播放 Facebook 视频：按视频列表逐个打开视频页 → 双侧抓包（本机 tshark + IAS 设备 dumpcap）→ 自动关闭登录弹窗、读取/切换画质 → 播放到预设时长（或视频播完）→ 停止抓包、关闭浏览器 → 把结果写入 Excel。全程记录日志；网络异常、WiFi 掉线、IP 被封堵时可自动重连 WiFi / 切换 IP（V1.2 新增 WiFi 自动重连）。

## 2. 运行环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / Windows Server（用到 netsh、注册表） |
| Python | 3.x |
| 依赖包 | `pip install ping3 psutil openpyxl paramiko xlsxwriter selenium undetected-chromedriver` |
| 浏览器 | 已安装 Chrome（版本号自动从注册表读取） |
| 抓包工具 | Wireshark（需用到其 tshark.exe，路径见配置 `WIRESHARK_PATH`） |
| 权限 | **建议以管理员身份运行**（切换静态 IP、启停网卡等 netsh 操作需要管理员权限） |

## 3. 运行前准备

### 3.1 检查配置（FB_PC_V1.2\config.py）

常用检查项（含义详见文件内注释）：

| 配置 | 说明 | 改动场景 |
|---|---|---|
| `WIFI_SSID` | 自动连接的目标 WiFi 名称 | 换测试 WiFi 时 |
| `INTERFACE` | 上网网卡名，默认 `WLAN` | 用有线网上网时 |
| `WIRESHARK_PATH` / `ETH_NAME` | Wireshark 目录 / 本机捕包网卡 GUID | 换抓包机时（文件里留有外网机备选注释） |
| `HOSTNAME` / `USERNAME` / `PASSWORD` / `TCPDUMP_PATH` / `IAS_ETH` | IAS 的 SSH 地址、账号与抓包程序路径 | 换 IAS 设备时 |
| `VID_FILE` | 视频列表路径 | 换播放任务时 |
| `IP_LIST_PATH` | IP 切换列表路径 | — |
| `IS_SCREEN` | `True`=无界面（headless）运行 | 服务器/无人值守场景 |
| `IS_IAS_PCAP` | `False`=不做 IAS 侧抓包 | 只需要本机抓包时 |
| `IS_IP_SWITCH` | `False`=不自动切 IP | 固定 IP 测试时 |
| `IS_TIME_LIMIT` | `True`=IP 复用需间隔 30 分钟 | IP 资源紧张时 |
| `FB_USERNAME` / `FB_PASSWORD` | Facebook 账号（当前入口未自动登录，仅 `login()` 备用） | — |

### 3.2 准备视频列表（fb视频列表_1.xlsx）

第 1 行为表头，数据从第 2 行开始，脚本读取 **B~E 列**：

| 行 | B：视频 vid | C：画质 | D：播放时长(秒) | E：状态 |
|---|---|---|---|---|
| 2 | 123456789012345 | 720P | 120 | （留空） |
| 3 | 987654321098765 | 自动 | （空则默认 60） | （留空） |

- vid 取自 Facebook 视频链接 `facebook.com/watch?v={vid}` 中的 `{vid}`；
- 画质可写 `720P`、`1080P`、`自动` 等（只取数字部分匹配，不区分大小写）；
- 播完后脚本自动在 E 列写"已播放"，**中断后重新运行会自动跳过已播条目、断点续跑**；想重播就把 E 列清空。

### 3.3 准备 IP 切换列表（ip_list.xlsx，开启 IS_IP_SWITCH 时需要）

| A：IP | B：网关 | C：上次使用时间 | D：是否已用 |
|---|---|---|---|
| 10.10.163.86 | 10.10.163.1 | （留空） | （留空） |

C、D 两列由脚本自动填写；列表用完会自动整列重置循环复用。

### 3.4 网络与设备前提

1. 本机通过 `INTERFACE`（默认 WLAN）上网，且系统里**保存过目标 WiFi 的配置文件**（手动连接一次并勾选"自动连接"），否则脚本无法自动重连 WiFi（会打印提示）；
2. IAS 设备（默认 192.168.80.93）网络可达、SSH 账号可用；不开 IAS 抓包（`IS_IAS_PCAP=False`）可忽略；
3. ⚠️ 代码中 IAS 的 SSH 连接把本机源地址写死为 `10.10.163.86`，若本机没有这个地址，IAS 操作会报错——遇到时请联系开发调整（详见实现说明第 7 节）。

## 4. 启动与停止

以管理员身份打开命令行：

```
cd /d E:\PC-Facebook\pythonProject\FB_PC_V1.2
python main.py
```

也可在任意目录执行 `python E:\PC-Facebook\pythonProject\FB_PC_V1.2\main.py`。

- 停止：`Ctrl+C`；再次运行即从"已播放"之后继续；
- 脚本启动和每轮播放前都会自动清理残留的 chrome / tshark 进程，**请注意保存 Chrome 中未提交的页面**。

## 5. 运行过程（每个视频一个周期）

1. 检查/重连 WiFi → 确认本机 IP 有效（无效且允许时自动从 ip_list 换 IP）；
2. 清理 IAS 与本机残留抓包进程 → 启动 IAS 抓包（等 PID 就绪）→ 启动本机 tshark 抓包；
3. 打开视频页：若提示"无法访问此网站"等错误，自动重连 WiFi、按需切 IP，本条记为失败并进入下一条；
4. 关闭登录弹窗 → 读取视频总时长与当前画质 → 尝试切到列表预设画质（菜单里没有该档位则保持当前画质）；
5. 播放监控：每秒读取进度，**进度 60 秒不动判为封堵**（重连 WiFi、按需切 IP、结束本条）；视频比预设时长短则播完即止；
6. 收尾：关浏览器 → 停 tshark → 停 IAS 抓包（确认进程退出）→ 按需切 IP → 结果写入 Excel、视频列表标记"已播放"。

## 6. 输出结果

| 位置 | 内容 |
|---|---|
| `E:\PC-Facebook\fb_result\YYYYMMDD\fb播放结果_时间戳.xlsx` | 结果表，13 列：vid、画质、实际播放画质、视频总时长、itag、是否封堵成功、预设时长、实际时长、开始/停止时间、播放IP、PCAP文件名、IAS_PCAP文件名 |
| `E:\PC-Facebook\fb_pcap\YYYYMMDD\*.pcap` | 本机抓包；IAS 侧包在 IAS 的 `/appslog/fb_pc_YYYYMMDD/` |
| 同目录 `*.key` | TLS 密钥文件；在 Wireshark 的 SSLKEYLOGFILE 配置中指定同名 .key 即可解密对应 pcap |
| `fb播放失败URL列表_时间戳.txt` | 播放异常条目及原因 |
| `E:\PC-Facebook\fb_log_时间戳.log` | 全程日志（与控制台输出一致） |
| 视频列表 / ip_list.xlsx | E 列"已播放"标记；IP 使用时间与标记 |

结果表中若某行为"无 / 视频链接打开失败 / IP设置不成功"等，表示该条未完成正常播放，原因见对应日志与失败列表。

## 7. 常见问题

| 现象 | 处理 |
|---|---|
| 日志提示"系统中不存在WiFi配置文件" | 手动连接一次该 WiFi 并勾选自动连接，再运行脚本 |
| IAS 相关操作报错 / 抓包启动失败 | 检查 IAS 是否可达、账号是否正确，以及本机是否存在 `10.10.163.86` 地址（见 3.4 第 3 条）；不需要 IAS 包时可设 `IS_IAS_PCAP=False` |
| 结果里实际画质=自动、时长=预设值 | 该条播放时未能关闭登录弹窗或未取到视频时长，走了保守等待逻辑，属正常容错行为 |
| 视频实际时长小于预设时长 | 视频本身比预设短，以播完为准 |
| 换 IP 后无法上网 | 检查 ip_list 中 IP/网关是否与测试网络匹配；必要时设 `IS_IP_SWITCH=False` |
| Chrome/tshark 残留进程 | 脚本每轮会自动清理；异常中断后可手动结束，或直接重跑脚本 |

## 8. 注意事项

1. 切换 IP、启停网卡需要**管理员权限**；
2. `config.py` 中 Facebook 账号与 IAS root 密码为**明文**，请勿外传该文件；
3. 运行期间会强制结束所有 Chrome 窗口，请勿在同一台机器同时做浏览器测试；
4. 同一台机器同时只跑一个实例（多实例会争抢网卡、抓包文件与 Chrome）。
